# 复制为 local.yaml，仅填写需要覆盖 defaults.yaml 的字段。
# 可以使用 YAML 锚点显式复用密钥；程序不会自动借用其他服务密钥。
embedding:
  api_key: ""
llm:
  qa:
    api_key: ""
  vision:
    api_key: ""
# auth:
#   password: "你的本机密码"
#   jwt_secret: "你的随机签名密钥"
# dataset:
#   t2_dir: dataset/T2Retrieval-subset
