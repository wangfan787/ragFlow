# RAG 评测：契约、实录与速查

更新日期：2026-09-19。本文是评测数据契约、真实运行记录与参数速查的唯一文档。项目总体优先级以 [`README.md`](README.md) 为准；算法概念见 [`算法层QA.md`](算法层QA.md)；面试口径见 [`docs/interview/评测与调参口径.md`](../interview/评测与调参口径.md)。本文内部阶段统一使用 `E0–E3`，与项目全局 P0/P1/P2 编号无关。

> **一句话结论（首次真实调参，2026-09-19）**：默认混合检索召回 88.2%、反被纯向量压着打；定位到融合层分数归一化污染后单变量调参，锁定配置在 120 道 Test 上召回 97.5%、反超纯向量 5 个点；回答侧 evidence recall 93.1%、引用正确率 96.7%、P95 延迟 2.3 秒——全程成本约两块钱，每个数字都能离线复算。

---

## 第一部分：数据与执行契约（E0–E3）

### 1.1 当前决策

主评测集使用 `CRUD-RAG-mini-v1`，不再继续扩建 T2Retrieval。首版固定为：

- 1,000 篇中文文本：全部正例文档 + 普通干扰项 + 相似主题困难干扰项。
- 150 条问答：`questanswer_1doc`、`questanswer_2docs`、`questanswer_3docs` 各 50 条。
- 30 条 Dev（每类 10 条）用于调参；120 条 Test（每类 40 条）只用于最终比较。
- 150 条问题使用不同的事件 ID，Dev/Test 间也不存在事件重叠。
- 固定上游 Git commit、`seed=42`、规范化算法版本和全部产物 SHA256。

选择这个规模的原因：它足以完成个人项目的固定同集 A/B、失败分析和面试讲解，又不会产生不必要的模型费用。它只能证明在这个受控子集上的相对效果，不能代表 8 万文档或企业级流量。

### 1.2 数据来源与边界

原始数据位于本地 `dataset/CRUD_RAG/`，整个目录必须被 Git 忽略。评测代码只读取数据，不安装或导入上游 CRUD-RAG 的旧版依赖。

构建时以 `data/crud_split/split_merged.json` 中问答样本自带的 `news1/news2/news3` 作为正例证据。上游 8 万文档没有可依赖的稳定 doc-id/qrels 映射，因此只作为干扰文档来源，不能通过模糊匹配猜测正例身份。

稳定标识：

```text
normalized_text = Unicode NFKC + CRLF/CR→LF + 行内空白折叠 + strip
doc_id          = "crud_" + sha256(normalized_text)[:24]
query_id        = "crud_q_" + sha256(task + event_id + question)[:24]
```

若两条正例规范化后相同，则共享同一个 `doc_id`；qrels 必须去重。普通/困难干扰项不得与任一正例完全相同或规范化后相同。

### 1.3 冻结产物

生成目录与 `backend/` 同级：

```text
evaluation/
├── README.md
├── build_dataset.py
├── validate_dataset.py
├── score_run.py
├── report.py
├── schemas.py
├── datasets/crud_rag_mini_v1/  # 本地生成，不提交 Git
├── runs/                         # 真实链路录制，不提交 Git
├── scores/                       # 离线评分结果，不提交 Git
├── reports/generated/            # 生成报告，不提交 Git
└── tests/
```

数据集目录包含：

```text
README.md
manifest.json
corpus.jsonl
queries.dev.jsonl
queries.test.jsonl
qrels.jsonl
references.jsonl
```

每条 query 保存 `query_id`、`task`、`event_id`、`question`、`answer` 和 `split`；每条 qrel 保存 `query_id`、`doc_id`、`relevance=1`。`references.jsonl` 保存问题到所需正例集合的映射，不能只保留答案文本。

### 1.4 评测运行契约

真实检索/问答链路只运行一次并录制 JSONL，确定性 scorer 可以反复离线计算。每条 run 至少保存：

- 数据集 ID、Git commit、配置指纹和模型名（不保存密钥）。
- `query_id`、检索候选的 `doc_id/rank/score/channel`。
- 实际进入 Prompt 的 evidence `doc_id`。
- 回答、引用 `doc_id`、状态和错误。
- 检索/生成/总延迟、输入/输出 token 和可得的成本。

E0 已实现的确定性指标：

- Retrieval：Hit@1/3/5/10、Recall@1/3/5/10、MRR@10、nDCG@10。
- Evidence：Evidence Recall、Evidence Precision；多文档问题必须按所需文档集合计算。
- Citation：Citation Validity 和 Citation Document Precision；E0 不伪装成 claim-level correctness。
- Performance：P50/P95 延迟、token/cost 汇总（字段不可得时明确记为 unavailable）。
- 分片：总分以及 1doc/2docs/3docs 三类分别报告。

E2 才引入 LLM Judge。首版固定抽取 Test 中 50 条（15/15/20）评 Faithfulness、Answer Correctness、Answer Relevancy 和 Context Precision/Recall；必须保存 rubric、模型版本、原始理由，并由人类抽查校准。

#### 1.4.1 Runner 与生产代码的边界

`evaluation/runner` 负责读取固定数据、选择实验配置、调用生产服务并录制结果；不得复制 `backend/` 的解析、分块、检索、窗口、生成或引用算法。需要改变真实产品行为的开关必须先进入生产配置：

| 生产接口 | 当前状态 | E1 剩余工作 | 已有验收证据 |
|---|---|---|---|
| 检索模式 | √ `RetrievalConfig.retrieval_mode = vector/keyword/hybrid` 已进入生产链路 | Runner 保存实际模式并调用同一生产入口 | 单测断言未选通道调用次数为 0，单通道分数使用有效权重 |
| Rerank 漏斗 | √ `rerank_top_n`、off/on、backend 和失败回退已实现 | Runner 保存实际漏斗并完成真实 A/B | 已校验 `top_k <= rerank_top_n <= candidate_top_k`；Trace 保存 backend/model/送排数/fallback |
| Evidence Window | √ 请求级 `QAConfig` 已支持 `child_only/window/full_parent` | Runner 在 Dev 传不同配置并原样录制 | 同一进程逐请求配置测试通过；Trace 保存最终生效值 |
| QA 可观测性 | √ 已返回请求级 `trace.config/timings_ms/usage` | Runner 原样写入 run schema，不从日志反推 | retrieval/window/generation/citation/total 和 usage 回归通过；不可得字段为 `unavailable` |

Runner 的建议结构：

```text
evaluation/runner/
├── production_adapter.py  # 将生产 payload 映射为已冻结 run schema
├── index_dataset.py       # 调用 IngestionPipeline，写独立评测索引
├── run_retrieval.py       # 调用 HybridRouter，不调用 Chat
├── run_qa.py              # 调用 QAService 完整链路
└── sweep.py               # 只在 Dev 编排单变量参数扫描
```

评测索引必须使用独立名称，并生成 `index_manifest.json`，至少包含 dataset manifest SHA、Git commit、ES index、Embedding 模型、chunk profile/version/hash、成功/失败文档数和 Chunk 数。CRUD-RAG 的原始 `doc_id` 必须保留，否则无法与 qrels 对齐。

### 1.5 E0：评测基础设施（已完成）

#### E0-A：冻结数据集

- [x] 实现确定性构建 CLI，固定 50/50/50、30/120、事件隔离和 `seed=42`。
- [x] 确保所有 qrels 正例都存在于 1,000 文档语料，且文档 ID 唯一。
- [x] 加入普通与相似主题干扰项；排除正例的规范化重复。
- [x] 写入 source commit、算法版本、参数、文件行数/字节数/SHA256。
- [x] 生成本地 `crud_rag_mini_v1`，并用独立 validator 验收。

#### E0-B：确定性评分闭环

- [x] 定义 run JSONL schema，拒绝未知 query/doc、重复排名和缺失必填字段。
- [x] 实现 Retrieval、Evidence、Citation、Performance scorer。
- [x] 按 overall/1doc/2docs/3docs 输出机器可读 JSON。
- [x] 从同一 score JSON 生成 Markdown 报告，避免手填指标。

#### E0-C：回归保护与可运行说明

- [x] 单测覆盖确定性、事件隔离、正例完备、hash 校验和指标手算样例。
- [x] 在 Conda `agent` 环境运行测试并记录真实结果。
- [x] 文档给出 build→validate→score→report 命令，不要求安装 CRUD-RAG 上游依赖。

E0 完成定义：任意人拿到同一上游 commit，运行构建命令得到相同的 1,000/150 数据、相同 hash；一个符合 schema 的 run 能离线得到确定性指标和报告。E0 不要求付费调用模型，也不虚构尚未运行的真实检索 baseline。

当前验收结果（2026-09-17）：E0 已完成。实际冻结数据包含 1,000 篇文档、150 条问题、300 条 qrels；Dev/Test 为 30/120；正例/困难干扰/随机干扰为 300/150/550。原始数据与生成数据均由 `.gitignore` 排除。

### 1.6 E1：生产链路接入与确定性 A/B（2026-09-19 已完成）

#### E1-A：生产接口前置

- [x] 增加严格隔离通道的 `retrieval_mode`，禁止用权重 0 冒充 Vector-only。
- [x] 增加 `rerank_top_n`，保留已有 off/on、backend 和失败回退。
- [x] 增加请求级 `QAConfig`，支持 `child_only/window/full_parent` 与窗口 token。
- [x] QA 返回请求级配置快照、分阶段耗时和真实可得模型 usage；正式调用使用请求局部 Trace，不依赖共享 `last_trace`。

E1-A 已于 2026-09-18 完成；当时里程碑为 `102 passed`，包含随后端产品闭环和查询预处理新增回归在内，2026-09-21 全量为 `164 passed`。四个接口改造不再单列为未完成项。

#### E1-B：索引与 Runner（2026-09-19 完成）

- [x] `index_dataset.py` 调用生产 `IngestionPipeline` 将文档写入独立评测索引，并生成索引 manifest（替身测试 + 真实 ES 20 篇 smoke：212 条记录、doc_id 原样保留、逐文档失败显式记录）；全量 1,000 篇已于 2026-09-19 完成索引。
- [x] `production_adapter.py` 只做生产组件组装与 payload→run schema 映射，不实现算法；`build_stack()` 让索引/检索/问答共享同一评测 store。
- [x] `run_retrieval.py` 调用生产 `HybridRouter`，录制候选、通道、分数、配置和检索延迟（vector/keyword/hybrid/hybrid_rerank 四个默认 baseline）。
- [x] `run_qa.py` 调用生产 `QAService`，录制实际 Evidence（`qa_budget.evidence_doc_ids`）、回答、引用、usage 和阶段耗时；child_only/window/full_parent 三种证据模式默认变体。
- [x] run 保存 dataset/index/Git/config/model 指纹（每行 `meta` 字段）；错误逐题记录（no_evidence/error），不静默丢题。
- [x] `sweep.py` 只在 Dev 做单变量扫描（检索层不调 Chat；证据层含 window tokens）并写 `evaluation/locks/locked_config.json` + `sweep_summary.md`；Rerank 决策规则：recall@10 增益 ≥ 1pp 且 mrr@10 不下降才默认开启。

E1-B 验收（2026-09-19 完成）：`evaluation/tests/test_runner.py` 8 项通过；全量 1,000 篇索引（9,430 条记录、0 失败）、Dev 扫描锁定与 Test 各一次运行均已真实执行、120 题 × 3 证据模式零失败。首次 baseline 数字与调参全过程见本文第二部分。

#### E1-C：实验纪律

1. [x] 工具已实现且真实执行（2026-09-19）：`sweep.py` 只在 Dev 上单变量扫描 `candidate_top_k/top_k/threshold/vector_weight/rerank_top_n/evidence window`（检索层 20 组合 + 基线 4 个），锁定 `candidate_top_k=10, top_k=10, threshold=0.2, vector_weight=0.75, rerank=off, evidence=full_parent`；Test 各一次运行完成，未依据 Test 回调参数。
2. 先做不调用 Chat 的 Vector、Hybrid Weighted Sum、Hybrid+Rerank 检索 A/B。
3. 根据 Recall/MRR/nDCG/P95 锁定检索配置，再运行完整 QA，避免为每个检索组合重复支付生成费用。
4. 锁定所有参数后，在 120 条 Test 上各运行一次最终 baseline/candidate；不得根据 Test 继续调参。
5. 输出机器可读 score、Markdown 报告、分片指标和失败案例；没有真实 run 不得填 baseline 数字。

#### E1-D：查询预处理 A/B（2026-09-21 代码完成，真实 A/B 待执行）

- [x] 多轮指代消解已接同步/SSE 主链：请求传入 `history`，服务按完整 user+assistant 轮次裁剪，失败回退原问题。
- [x] 口语规范化已做成请求级开关；无历史时可执行，默认关闭，不改变既有 baseline。
- [x] Step-back 已做成默认关闭的请求级开关：背景问题与原问题分别走生产 Retriever，按 chunk 去重合并并记录 `trace.step_back`。
- [ ] 固定口语、多轮指代、歧义和背景原理四个切片；保存 query ID，禁止根据 Test 结果改样例。
- [ ] 比较原问题、普通改写、口语规范化、Step-back 的 Recall/MRR、意图保持、误改写率、P95 和单题成本。
- [ ] 未证明稳定收益前，口语规范化和 Step-back 保持默认关闭；歧义澄清尚未实现，不能把猜测式改写算成功。

### 1.7 E2：LLM Judge 与人工校准（待办）

- [ ] 固定 50 个 Test query ID：1doc/2docs/3docs 为 15/15/20，并将清单纳入版本控制。
- [ ] Judge 输入 question、reference answer/docs、实际 evidence/answer/citations；严格 JSON 输出评分、理由和 unsupported claims。
- [ ] 保存 rubric、Judge prompt/model/version、原始响应和解析错误。
- [ ] 由人类先独立复核 10–15 条以及全部明显分歧/低分样例，调整 rubric 后再冻结结果；AI 不能冒充人工校准。
- [ ] 只把经过校准的 Faithfulness、Answer Correctness、Answer Relevancy、Context Precision/Recall 写进正式报告。

### 1.8 E3：失败证据触发后再做

只有 E1/E2 的失败数据证明必要时，才扩大语料/问题规模、实现 Weighted RRF、增加不可回答/安全/图片/权限业务集，或引入第三方评测框架。当前不建设 Agent 评测。

---

## 第二部分：首次真实调参实录（2026-09-19）

本部分记录 `crud_rag_mini_v1` 上第一次真实评测调参全过程，按"问题 → 定位 → 尝试 → 结果"组织。

### 2.1 问题：理论上最强的 hybrid，实测反而拉胯

四种检索模式、生产默认参数（`top_k=5, candidate_top_k=30, threshold=0.1, 权重 0.75/0.25, rerank off`），Dev 30 题基线：

| Dev（30 题） | Recall@10 | MRR@10 | nDCG@10 |
|---|---:|---:|---:|
| vector 纯向量 | **0.9556** | **0.9167** | **0.9126** |
| keyword 纯 BM25 | 0.9444 | 0.8778 | 0.8678 |
| hybrid 默认混合 | 0.8667 | 0.8861 | 0.8355 |
| hybrid_rerank 混合+规则重排 | 0.9222 | 0.8500 | 0.8600 |

教科书说混合检索取长补短，实测比单通道还差 8.9 个点。预期和实测相反——这就是要讲的问题。

### 2.2 定位：四步推理链，实验反证闭环

1. **排除通道本身**：keyword 单独跑 recall 0.944，不弱 → 坏的不是 BM25，是融合这一步。
2. **看数据特性**：新闻问答，问法和正文用词大量重叠，两条通道命中高度重合 → 只解释"没赚"，不解释"倒亏"，继续往下挖。
3. **读融合代码找到机制**：融合分 = `0.75×向量 + 0.25×关键词`，关键词分按"本题最高分=1"归一 → 每道题必然有一个文档白得 0.25 加分；新闻高频词（记者/大会/年份）让 BM25 头名经常是错文档 → 错文档 0.30(向量)+0.25(白送) > 对文档 0.55(只在向量命中) → **正例被挤出前十，倒亏就是这么来的**。
4. **实验反证**：候选池 10/30/50/100 → recall 0.944 / 0.867 / 0.822 / 0.889，池子越大、弱关键词命中越多、污染越重——走势和推断一致，定位闭环。

### 2.3 尝试：三个方向，一个失败但有用

先立规矩再动手（防"过拟合式调参"）：只用 30 道 Dev 调、一次只动一个旋钮共 20 组（拒绝 864 种全组合）、选择标准事前定死（recall@10 优先，同分看 mrr@10）、120 道 Test 锁住只在最后跑一次。

**方向一：沿根因治理（有效，最终采纳）**

| 轴 | 各档位 recall@10 | 最优及理由 |
|---|---|---|
| candidate_top_k | 10→**0.9444**；30→0.8667；50→0.8222；100→0.8889 | 10：少捞弱关键词命中 |
| top_k | 3→0.7778；5→0.8667；10→**0.9444** | 10：名额直接抬召回 |
| similarity_threshold | 0→0.8556；0.05→0.8556；0.1→0.8667；0.2→**0.8944** | 0.2：低分噪声直接扔 |

**方向二：调融合权重 0.5~0.9 共 6 档（失败，但反向确认定位正确）**

recall 全卡在 0.84~0.87 的平台上，动权重没用——说明瓶颈不在权重比例，在归一化机制，定位对了。顺势的技术判断：RRF（按名次融合、免疫分数刻度问题）能治这个病，但便宜的参数治理已经把收益拿满，按"失败证据触发"原则不提前上复杂方案。

**方向三：规则重排（有效但按事前规则放弃）**

recall 0.8667→0.9222（+5.6pp），但 MRR 0.8861→0.8500（-3.6pp）。QA 场景只有前几名进提示词，头部顺序比池尾召回值钱；规则是事前定的（recall 增益 ≥1pp 且 MRR 不降才默认开），不是看着结果挑的。Test 上 hybrid_rerank 0.9569 也确实没超过锁定配置 0.9750，事后看决策正确。

### 2.4 结果：锁定配置 Test 一次验货，97.5%

锁定配置（通过生产 `build_retrieval_config` 校验）：

```json
{"candidate_top_k": 10, "top_k": 10, "similarity_threshold": 0.2, "vector_weight": 0.75, "rerank_enabled": false}
```

Dev 整体复验 recall@10 = 0.9722（各轴收益确实叠加，单轴最高只有 0.9444）。Test 120 题只跑一次、不回头调：

| Test（120 题） | Recall@10 | MRR@10 | nDCG@10 |
|---|---:|---:|---:|
| vector | 0.9236 | **0.9040** | 0.8725 |
| keyword | 0.8819 | 0.8153 | 0.8026 |
| hybrid（默认参数） | 0.8819 | 0.8432 | 0.8203 |
| hybrid_rerank | 0.9569 | 0.8953 | 0.8911 |
| **locked（锁定 hybrid）** | **0.9750** | 0.8913 | 0.8895 |

调参后的 hybrid 比默认多找回约 11 篇/120 题，反超纯向量 5.1pp。MRR 略低于 vector（0.8913 vs 0.9040）是阈值挡掉个别低分高排名候选的代价；业务若更在意第一名正确率，把选择标准换成 MRR 优先重扫一轮即可——选择标准是旋钮，不是教条。

### 2.5 下半场：证据模式与回答质量（DeepSeek）

**Dev 扫描（5 配置 × 30 题，选型）**

检索调到天花板后扫证据构造。evidence_recall 五种配置全部 0.8667——被检索上限压住，再次验证"Recall 决定天花板"；差异在引用质量：

| 证据配置（Dev） | citation_validity | P95 (ms) | input tokens/30 题 |
|---|---:|---:|---:|
| child_only | 0.7667 | 1927 | 15,269 |
| window 256 | 0.9000 | 2348 | 58,791 |
| window 384 | 0.8667 | 2297 | 64,982 |
| window 512 | 0.8667 | 2016 | 47,986 |
| **full_parent** | **0.9333** | 1905 | 42,393 |

锁定 **full_parent**：引用正确率最高、延迟最低（父块直接命中不用拼窗口）。

**Test 正式 run（锁定检索配置 × 3 种证据模式 × 120 题）**

120 题 × 3 变体全部 `ok`，零失败、零 no_evidence：

| Test（120 题） | evidence_recall | citation_validity | 引用文档精度 | P50 / P95 延迟 | input tokens/120 题 |
|---|---:|---:|---:|---:|---:|
| locked_child_only | 0.9306 | 0.8167 | 0.6647 | 1.06s / 1.81s | 56,013 |
| locked_window | 0.9306 | 0.9417 | 0.8579 | 1.24s / 2.41s | 205,231 |
| **locked_full_parent** | **0.9306** | **0.9667** | **0.8736** | 1.23s / 2.29s | 159,296 |

三个结论：

1. **evidence_recall 0.9306 三者相同**——证据召回由检索决定，证据模式只决定"给模型看的文本够不够答题"，这一层再次和 §2.2 的"Recall 决定天花板"互相印证；
2. **full_parent 双赢**：引用正确率最高（0.9667），且输入 token 比 window-384 少 22%（父块直接给，比拼窗口更省）——Dev 选型在 Test 上成立；
3. **child_only 的 token 省得不值**：输入只有 1/3，但引用正确率掉 15 个点；每题省约 ¥0.004，质量损失不可接受。

最终全链路（锁定检索 + full_parent）：**召回 97.5%，evidence recall 93.1%，引用正确率 96.7%，P95 2.3s**。

**成本**：全量索引 + 全部评测 ≈ ¥2（索引走 GLM embedding-3；生成走 DeepSeek，Dev 实测 150 次调用 22.9 万输入 + 1.8 万输出 token ≈ ¥0.6）。

### 2.6 复算路径

数字不是嘴说的：`evaluation/runs/*.jsonl`（每行含 query/retrieved/answer/usage/meta 指纹）、`evaluation/scores/*.json`、`evaluation/locks/locked_config.json`。复算命令见 `evaluation/README.md`。

---

## 第三部分：指标与参数速查

用途：忘了某个参数/指标是什么意思、默认值多少、锁定了哪个，翻这一部分。试错过程见第二部分，执行契约见第一部分。

### 3.1 检索漏斗参数（RetrievalConfig）

数据流：问题 → 每通道捞 candidate_top_k 个 → 加权融合 → 阈值过滤 → 可选重排 → 交出 top_k 条。

| 参数 | 默认值 | 锁定值 | 含义 | 调大/调小的效果 |
|---|---|---|---|---|
| `retrieval_mode` | hybrid | hybrid | 通道门控：vector 只走向量 / keyword 只走 BM25 / hybrid 两路都走。严格消融靠它，不是把权重设 0 | — |
| `candidate_top_k` | 30 | **10** | 每条通道捞多少候选（海选池大小） | 调大→召回池深但易混入噪声（本次污染来源） |
| `top_k` | 5 | **10** | 最终返回条数（QA 时 = 最多几块证据候选） | 调大→召回涨、精度可能摊薄 |
| `similarity_threshold` | 0.1 | **0.2** | 融合分及格线，低于直接淘汰 | 调大→噪声少但可能误杀低分正例 |
| `vector_weight` / `keyword_weight` | 0.75 / 0.25 | 0.75 / 0.25（未动） | 融合分 = 向量分×权重 + 关键词分×权重（各通道 0~1 归一） | 本次扫 6 档是平台 → 瓶颈不在此 |
| `rerank_enabled` | false | false（决策关闭） | 是否做精细二次排序 | recall +5.6pp 但 MRR -3.6pp，QA 重头部 → 按事前规则放弃 |
| `rerank_backend` | rule | — | rule（免费规则）/ cross-encoder（模型，中文需显式配模型） | — |
| `rerank_top_n` | None（=整个池） | — | 送多少候选去重排，约束 top_k ≤ rerank_top_n ≤ candidate_top_k | — |
| `filters` | None | — | 元数据过滤（如按 doc_id） | — |

### 3.2 QA 证据参数（QAConfig，请求级）

| 参数 | 默认值 | 锁定值 | 含义 |
|---|---|---|---|
| `evidence_mode` | window | **full_parent** | 喂给回答模型的证据切法：child_only 只给命中小碎块 / window 以命中点为中心扩窗口 / full_parent 整块父块 |
| `evidence_window_tokens` | 384 | — | window 模式的窗口大小（本次扫 256/384/512，full_parent 胜出） |
| `context_top_k` | 5 | — | 最多几块证据进提示词 |
| 总预算 | 32768 | — | llm.qa.context_limit_tokens − 输出预留 1024 − 安全余量 256 = 证据可用 token |

### 3.3 分块与嵌入参数

| 参数 | 值 | 含义 |
|---|---|---|
| parent_target / max tokens | 512 / 768 | 父块目标/硬上限 token |
| child_target / max tokens | 128 / 192 | 子块目标/硬上限（检索命中的最小单位） |
| embedding_input_budget | 192 | 子块嵌入输入上限，超了报错 |
| 原子块 | code / table / image | 不与相邻块合并；image 一图一块，超长只切描述文字不切像素 |
| profile | parent-child-v2 + sha256 前 12 位 | 进入 chunk_id，参数变了 ID 就变，防新旧混检 |
| Embedding | embedding-3，1024 维，只嵌子块正文（profile: child-body-v1） | 父块只存上下文不带向量 |
| 索引 | ES；生产 `rag-mvp-chunks`，评测 `rag-eval-crud_rag_mini_v1`（9,430 条/1,000 篇） | 向量持久化，调参不用重嵌语料 |

### 3.4 评测数据集（crud_rag_mini_v1，冻结）

| 项 | 值 |
|---|---|
| 语料 | 1,000 篇新闻 = 300 正例 + 150 相似困难干扰 + 550 随机干扰 |
| 问题 | 150 条 = 1doc/2docs/3docs 各 50；Dev 30（调参）/ Test 120（只验货） |
| qrels | 300 条（每题正例文档集合） |
| 固定 | 上游 commit、seed=42、全文件 SHA256；doc_id = "crud_"+sha256 前 24 位 |

### 3.5 指标含义速查（见到指标 → 它回答什么问题）

| 指标 | 回答什么问题 | 及格直觉 |
|---|---|---|
| Hit@K | 前 K 里至少命中一个正例吗 | 看下限 |
| Recall@K | 该找到的文档，找到几成 | 检索侧主指标，调参第一标准 |
| MRR@10 | 第一个正确结果排多靠前（第 1 名=1，第 2 名=0.5…） | QA 场景比 recall 更值钱：只有头部进提示词 |
| nDCG@10 | 整体排序质量（多个正例的顺序合不合理） | 综合参考 |
| Evidence Recall | 回答所需的关键事实进了证据没有 | 被检索 recall 封顶（本次两者同为 0.867/天花板现象） |
| Citation Validity | 标注的引用确实来自给出的证据吗 | 生成侧诚实度；child_only 0.77 vs full_parent 0.93 |
| Citation Document Precision | 引用的文档是正例吗 | 引用准不准 |
| P50/P95 延迟 | 一半/95% 请求多快 | P95 看长尾 |
| usage（input/output tokens） | 真实消耗，算成本用 | 不可得记 unavailable，不补零 |
| source_span accuracy | 来源坐标精度：exact 可逐字高亮 / line_only 只到行 / unavailable 不画假高亮 | 诚实分级，不冒充 exact |

真实成绩单见第二部分 §2.4/§2.5 的表格，此处不再重复。

### 3.6 模型与环境

| 用途 | 模型 | 端点 | 备注 |
|---|---|---|---|
| Embedding | embedding-3（1024 维） | bigmodel api/paas/v4 | key 在 .env |
| QA 生成 | deepseek-flash | api.deepseek.com | 已接受 thinking 参数，无需改代码 |
| Vision（图片描述） | glm-4.6v-flash | bigmodel api/paas/v4 | key 回退用 embedding key；P0-A 已实现，真实联调归 P1 |
| 运行环境 | conda `agent`（miniforge3） | — | `python -m pytest backend/tests evaluation/tests -q`；当前全量结果见 [`README.md`](README.md) 状态节（2026-09-21 为 `164 passed`） |
| 命令 | 索引/检索/问答/扫描 runner | — | 见 `evaluation/README.md` §3 |
