# RAG 评测实施计划

更新日期：2026-09-19。

本文是评测数据、生产 Runner、运行文件和评分口径的执行契约。项目总体优先级仍以 [`README.md`](README.md) 为准；算法概念和生产接口改造原因见 [`算法层QA.md`](算法层QA.md)。为避免与项目全局 P0/P1 混淆，本文内部阶段统一使用 `E0–E3`。

## 1. 当前决策

主评测集使用 `CRUD-RAG-mini-v1`，不再继续扩建 T2Retrieval。首版固定为：

- 1,000 篇中文文本：全部正例文档 + 普通干扰项 + 相似主题困难干扰项。
- 150 条问答：`questanswer_1doc`、`questanswer_2docs`、`questanswer_3docs` 各 50 条。
- 30 条 Dev（每类 10 条）用于调参；120 条 Test（每类 40 条）只用于最终比较。
- 150 条问题使用不同的事件 ID，Dev/Test 间也不存在事件重叠。
- 固定上游 Git commit、`seed=42`、规范化算法版本和全部产物 SHA256。

选择这个规模的原因：它足以完成个人项目的固定同集 A/B、失败分析和面试讲解，又不会产生不必要的模型费用。它只能证明在这个受控子集上的相对效果，不能代表 8 万文档或企业级流量。

## 2. 数据来源与边界

原始数据位于本地 `dataset/CRUD_RAG/`，整个目录必须被 Git 忽略。评测代码只读取数据，不安装或导入上游 CRUD-RAG 的旧版依赖。

构建时以 `data/crud_split/split_merged.json` 中问答样本自带的 `news1/news2/news3` 作为正例证据。上游 8 万文档没有可依赖的稳定 doc-id/qrels 映射，因此只作为干扰文档来源，不能通过模糊匹配猜测正例身份。

稳定标识：

```text
normalized_text = Unicode NFKC + CRLF/CR→LF + 行内空白折叠 + strip
doc_id          = "crud_" + sha256(normalized_text)[:24]
query_id        = "crud_q_" + sha256(task + event_id + question)[:24]
```

若两条正例规范化后相同，则共享同一个 `doc_id`；qrels 必须去重。普通/困难干扰项不得与任一正例完全相同或规范化后相同。

## 3. 冻结产物

生成目录与 `backend/` 同级：

```text
evaluation/
├── README.md
├── build_dataset.py
├── validate_dataset.py
├── score_run.py
├── report.py
├── schemas.py
├── datasets/crud_rag_mini_v1/  # 本地生成，不提交 Git
├── runs/                         # 真实链路录制，不提交 Git
├── scores/                       # 离线评分结果，不提交 Git
├── reports/generated/            # 生成报告，不提交 Git
└── tests/
```

数据集目录包含：

```text
README.md
manifest.json
corpus.jsonl
queries.dev.jsonl
queries.test.jsonl
qrels.jsonl
references.jsonl
```

每条 query 保存 `query_id`、`task`、`event_id`、`question`、`answer` 和 `split`；每条 qrel 保存 `query_id`、`doc_id`、`relevance=1`。`references.jsonl` 保存问题到所需正例集合的映射，不能只保留答案文本。

## 4. 评测运行契约

真实检索/问答链路只运行一次并录制 JSONL，确定性 scorer 可以反复离线计算。每条 run 至少保存：

- 数据集 ID、Git commit、配置指纹和模型名（不保存密钥）。
- `query_id`、检索候选的 `doc_id/rank/score/channel`。
- 实际进入 Prompt 的 evidence `doc_id`。
- 回答、引用 `doc_id`、状态和错误。
- 检索/生成/总延迟、输入/输出 token 和可得的成本。

E0 已实现的确定性指标：

- Retrieval：Hit@1/3/5/10、Recall@1/3/5/10、MRR@10、nDCG@10。
- Evidence：Evidence Recall、Evidence Precision；多文档问题必须按所需文档集合计算。
- Citation：Citation Validity 和 Citation Document Precision；E0 不伪装成 claim-level correctness。
- Performance：P50/P95 延迟、token/cost 汇总（字段不可得时明确记为 unavailable）。
- 分片：总分以及 1doc/2docs/3docs 三类分别报告。

E2 才引入 LLM Judge。首版固定抽取 Test 中 50 条（15/15/20）评 Faithfulness、Answer Correctness、Answer Relevancy 和 Context Precision/Recall；必须保存 rubric、模型版本、原始理由，并由人类抽查校准。

### 4.1 Runner 与生产代码的边界

`evaluation/runner` 负责读取固定数据、选择实验配置、调用生产服务并录制结果；不得复制 `backend/` 的解析、分块、检索、窗口、生成或引用算法。需要改变真实产品行为的开关必须先进入生产配置：

| 生产接口 | 当前状态 | E1 剩余工作 | 已有验收证据 |
|---|---|---|---|
| 检索模式 | √ `RetrievalConfig.retrieval_mode = vector/keyword/hybrid` 已进入生产链路 | Runner 保存实际模式并调用同一生产入口 | 单测断言未选通道调用次数为 0，单通道分数使用有效权重 |
| Rerank 漏斗 | √ `rerank_top_n`、off/on、backend 和失败回退已实现 | Runner 保存实际漏斗并完成真实 A/B | 已校验 `top_k <= rerank_top_n <= candidate_top_k`；Trace 保存 backend/model/送排数/fallback |
| Evidence Window | √ 请求级 `QAConfig` 已支持 `child_only/window/full_parent` | Runner 在 Dev 传不同配置并原样录制 | 同一进程逐请求配置测试通过；Trace 保存最终生效值 |
| QA 可观测性 | √ 已返回请求级 `trace.config/timings_ms/usage` | Runner 原样写入 run schema，不从日志反推 | retrieval/window/generation/citation/total 和 usage 回归通过；不可得字段为 `unavailable` |

Runner 的建议结构：

```text
evaluation/runner/
├── production_adapter.py  # 将生产 payload 映射为已冻结 run schema
├── index_dataset.py       # 调用 IngestionPipeline，写独立评测索引
├── run_retrieval.py       # 调用 HybridRouter，不调用 Chat
├── run_qa.py              # 调用 QAService 完整链路
└── sweep.py               # 只在 Dev 编排单变量参数扫描
```

评测索引必须使用独立名称，并生成 `index_manifest.json`，至少包含 dataset manifest SHA、Git commit、ES index、Embedding 模型、chunk profile/version/hash、成功/失败文档数和 Chunk 数。CRUD-RAG 的原始 `doc_id` 必须保留，否则无法与 qrels 对齐。

## 5. E0：评测基础设施（已完成）

### E0-A：冻结数据集

- [x] 实现确定性构建 CLI，固定 50/50/50、30/120、事件隔离和 `seed=42`。
- [x] 确保所有 qrels 正例都存在于 1,000 文档语料，且文档 ID 唯一。
- [x] 加入普通与相似主题干扰项；排除正例的规范化重复。
- [x] 写入 source commit、算法版本、参数、文件行数/字节数/SHA256。
- [x] 生成本地 `crud_rag_mini_v1`，并用独立 validator 验收。

### E0-B：确定性评分闭环

- [x] 定义 run JSONL schema，拒绝未知 query/doc、重复排名和缺失必填字段。
- [x] 实现 Retrieval、Evidence、Citation、Performance scorer。
- [x] 按 overall/1doc/2docs/3docs 输出机器可读 JSON。
- [x] 从同一 score JSON 生成 Markdown 报告，避免手填指标。

### E0-C：回归保护与可运行说明

- [x] 单测覆盖确定性、事件隔离、正例完备、hash 校验和指标手算样例。
- [x] 在 Conda `agent` 环境运行测试并记录真实结果；2026-09-19 当前全量结果为 `102 passed`。
- [x] 文档给出 build→validate→score→report 命令，不要求安装 CRUD-RAG 上游依赖。

E0 完成定义：任意人拿到同一上游 commit，运行构建命令得到相同的 1,000/150 数据、相同 hash；一个符合 schema 的 run 能离线得到确定性指标和报告。E0 不要求付费调用模型，也不虚构尚未运行的真实检索 baseline。

当前验收结果（2026-09-17）：E0 已完成。实际冻结数据包含 1,000 篇文档、150 条问题、300 条 qrels；Dev/Test 为 30/120；正例/困难干扰/随机干扰为 300/150/550。原始数据与生成数据均由 `.gitignore` 排除。

## 6. E1：生产链路接入与确定性 A/B

### E1-A：生产接口前置

- [x] 增加严格隔离通道的 `retrieval_mode`，禁止用权重 0 冒充 Vector-only。
- [x] 增加 `rerank_top_n`，保留已有 off/on、backend 和失败回退。
- [x] 增加请求级 `QAConfig`，支持 `child_only/window/full_parent` 与窗口 token。
- [x] QA 返回请求级配置快照、分阶段耗时和真实可得模型 usage；正式调用使用请求局部 Trace，不依赖共享 `last_trace`。

E1-A 已于 2026-09-18 完成；2026-09-19 在 Conda `agent` 环境重跑后端与评测测试，结果为 `102 passed`。E1 当前只剩 Runner、真实索引/run 和 A/B 报告，不再把四个接口改造重复列为未完成项。

### E1-B：索引与 Runner（2026-09-19 代码完成，真实全量 run 待执行）

- [x] `index_dataset.py` 调用生产 `IngestionPipeline` 将文档写入独立评测索引，并生成索引 manifest（替身测试 + 真实 ES 20 篇 smoke：212 条记录、doc_id 原样保留、逐文档失败显式记录）。全量 1,000 篇待执行。
- [x] `production_adapter.py` 只做生产组件组装与 payload→run schema 映射，不实现算法；`build_stack()` 让索引/检索/问答共享同一评测 store。
- [x] `run_retrieval.py` 调用生产 `HybridRouter`，录制候选、通道、分数、配置和检索延迟（vector/keyword/hybrid/hybrid_rerank 四个默认 baseline）。
- [x] `run_qa.py` 调用生产 `QAService`，录制实际 Evidence（`qa_budget.evidence_doc_ids`）、回答、引用、usage 和阶段耗时；child_only/window/full_parent 三种证据模式默认变体。
- [x] run 保存 dataset/index/Git/config/model 指纹（每行 `meta` 字段）；错误逐题记录（no_evidence/error），不静默丢题。
- [x] `sweep.py` 只在 Dev 做单变量扫描（检索层不调 Chat；证据层含 window tokens）并写 `evaluation/locks/locked_config.json` + `sweep_summary.md`；Rerank 决策规则：recall@10 增益 ≥ 1pp 且 mrr@10 不下降才默认开启。

E1-B 验收（2026-09-19 完成）：`evaluation/tests/test_runner.py` 8 项通过；全量 1,000 篇索引（9,430 条记录、0 失败）、Dev 扫描锁定与 Test 各一次运行均已真实执行、120 题 × 3 证据模式零失败。首次 baseline 数字与调参全过程见 [`调参实录.md`](调参实录.md)。

### E1-C：实验纪律

1. [x] 工具已实现且真实执行（2026-09-19）：`sweep.py` 只在 Dev 上单变量扫描 `candidate_top_k/top_k/threshold/vector_weight/rerank_top_n/evidence window`（检索层 20 组合 + 基线 4 个），锁定 `candidate_top_k=10, top_k=10, threshold=0.2, vector_weight=0.75, rerank=off, evidence=full_parent`；Test 各一次运行完成，未依据 Test 回调参数。
2. 先做不调用 Chat 的 Vector、Hybrid Weighted Sum、Hybrid+Rerank 检索 A/B。
3. 根据 Recall/MRR/nDCG/P95 锁定检索配置，再运行完整 QA，避免为每个检索组合重复支付生成费用。
4. 锁定所有参数后，在 120 条 Test 上各运行一次最终 baseline/candidate；不得根据 Test 继续调参。
5. 输出机器可读 score、Markdown 报告、分片指标和失败案例；没有真实 run 不得填 baseline 数字。

## 7. E2：LLM Judge 与人工校准

- [ ] 固定 50 个 Test query ID：1doc/2docs/3docs 为 15/15/20，并将清单纳入版本控制。
- [ ] Judge 输入 question、reference answer/docs、实际 evidence/answer/citations；严格 JSON 输出评分、理由和 unsupported claims。
- [ ] 保存 rubric、Judge prompt/model/version、原始响应和解析错误。
- [ ] 由人类先独立复核 10–15 条以及全部明显分歧/低分样例，调整 rubric 后再冻结结果；AI 不能冒充人工校准。
- [ ] 只把经过校准的 Faithfulness、Answer Correctness、Answer Relevancy、Context Precision/Recall 写进正式报告。

## 8. E3：失败证据触发后再做

只有 E1/E2 的失败数据证明必要时，才扩大语料/问题规模、实现 Weighted RRF、增加不可回答/安全/图片/权限业务集，或引入第三方评测框架。当前不建设 Agent 评测。

## 9. 面试口径

> 我已经从 CRUD-RAG 固定构建 1,000 篇文档和 150 条分层问答，1/2/3 文档问题各 50 条。所有正例强制入库，再加入普通和困难干扰项；Dev/Test 按事件 ID 隔离，并固定上游 commit、随机种子、算法版本和文件哈希。当前已完成数据与确定性 scorer；生产 Runner、真实 baseline 和经人工校准的 Judge 结果完成后才会报告对应分数。这个 Mini Benchmark 用于低成本、可复现的方案比较，不宣称等价于全量或线上效果。
